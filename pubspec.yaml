name: sticky_notes_viewer
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  desktop_multi_window:
    path: third_party/desktop_multi_window
  easy_localization: ^3.0.8
  ffi: ^2.1.0
  flutter:
    sdk: flutter
  flutter_markdown_plus: ^1.0.12
  win32: ^6.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/translations/
